apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-api
  labels:
    app: shop-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shop-api
  template:
    metadata:
      labels:
        app: shop-api
    spec:
      containers:
        - name: shop-api
          image: faraji88/shop-api:master
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: shop-api-secret
